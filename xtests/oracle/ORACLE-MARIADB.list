# A10 — fixtures that run on the MariaDB leg (cross-dialect SQL only).
# orig.spg_jsonb_at_op excluded: JSONB type + `@>` are PG surface;
# MySQL-family spells it JSON + JSON_CONTAINS() (dialect fixture pending).
port.select_implicit.sql
port.subquery_correlated_agg.sql
orig.collation_pad_matrix.sql
orig.mysql_family_literals.sql
