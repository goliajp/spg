# PG array operators: ANY-form membership, concat ||.

statement ok
CREATE TABLE t (id INT NOT NULL, tags TEXT[])

statement ok
INSERT INTO t VALUES (1, ARRAY['a', 'b']), (2, ARRAY['b', 'c'])

# tags column round-trips with PG canonical form.
query IT rowsort
SELECT id, tags FROM t ORDER BY id
----
1
{a,b}
2
{b,c}

# 7.38.1 S4.2 — cleanup discipline: leave nothing behind.
statement ok
DROP TABLE IF EXISTS t CASCADE
