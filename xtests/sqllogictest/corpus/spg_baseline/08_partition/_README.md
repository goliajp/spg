# 08 — Partition

PARTITION BY RANGE — declarative partitioning. v7.37.6-B is the
target train for this surface (per active-roadmap.md). Files arrive
when that train ships its parser + executor support.
