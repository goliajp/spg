# SERIAL / BIGSERIAL implicit identity columns. v7.29.0 BIGSERIAL fix.
# SERIAL implies NOT NULL — no need to spell it out.

statement ok
CREATE TABLE t (id SERIAL PRIMARY KEY, name TEXT NOT NULL)

statement ok
INSERT INTO t (name) VALUES ('a'), ('b'), ('c')

query I rowsort
SELECT COUNT(*) FROM t
----
3

# 7.38.1 S4.2 — cleanup discipline: leave nothing behind.
statement ok
DROP TABLE IF EXISTS t CASCADE
