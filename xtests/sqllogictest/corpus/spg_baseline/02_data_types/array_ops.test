# Array indexing + array literal round-trip.

statement ok
CREATE TABLE t (id INT NOT NULL, tags TEXT[])

statement ok
INSERT INTO t VALUES (1, ARRAY['x', 'y', 'z']), (2, ARRAY['p'])

# Array stored as canonical PG text {x,y,z}.
query IT rowsort
SELECT id, tags FROM t ORDER BY id
----
1
{x,y,z}
2
{p}

# 7.38.1 S4.2 — cleanup discipline: leave nothing behind.
statement ok
DROP TABLE IF EXISTS t CASCADE
