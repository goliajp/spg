# CREATE TYPE AS ENUM.

statement ok
CREATE TYPE mood AS ENUM ('sad', 'ok', 'happy')

statement ok
CREATE TABLE t (id INT NOT NULL, m mood)

statement ok
INSERT INTO t VALUES (1, 'happy'), (2, 'sad'), (3, 'ok')

query IT rowsort
SELECT id, m FROM t ORDER BY id
----
1
happy
2
sad
3
ok
