# INNER JOIN ON.

statement ok
CREATE TABLE a (id INT NOT NULL, name TEXT NOT NULL)

statement ok
CREATE TABLE b (a_id INT NOT NULL, val INT NOT NULL)

statement ok
INSERT INTO a VALUES (1, 'x'), (2, 'y'), (3, 'z')

statement ok
INSERT INTO b VALUES (1, 100), (1, 101), (2, 200)

query ITI rowsort
SELECT a.id, a.name, b.val FROM a INNER JOIN b ON a.id = b.a_id
----
1
x
100
1
x
101
2
y
200
