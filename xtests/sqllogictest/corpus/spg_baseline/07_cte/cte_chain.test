# CTE referencing earlier CTE.

statement ok
CREATE TABLE t (id INT NOT NULL, v INT NOT NULL)

statement ok
INSERT INTO t VALUES (1, 10), (2, 20), (3, 30), (4, 40)

query II rowsort
WITH big AS (SELECT id, v FROM t WHERE v >= 20), top AS (SELECT id, v FROM big WHERE v >= 30) SELECT id, v FROM top
----
3
30
4
40

# 7.38.1 S4.2 — cleanup discipline: leave nothing behind.
statement ok
DROP TABLE IF EXISTS t CASCADE
