[package]
name         = "spg-manifest"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
authors      = { workspace = true }
license      = { workspace = true }
description  = "v7.1 — SPGMAN01 v10 catalog manifest format. Frozen wire/disk surface shared by spg-server (boot, CHECKPOINT, segment forwarding) and spg-embedded (open_path durability)."

[dependencies]
spg-crypto = { path = "../spg-crypto" }

[lints]
workspace = true
