[package]
name         = "spg-server"
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
authors      = { workspace = true }
license      = { workspace = true }
description  = "SPG daemon — listens for self-built wire-frame connections."

[[bin]]
name = "spg-server"
path = "src/main.rs"

[dependencies]
spg-wire    = { path = "../spg-wire" }
spg-engine  = { path = "../spg-engine" }
spg-storage = { path = "../spg-storage" }
spg-audit   = { path = "../spg-audit" }

[dev-dependencies]
spg-audit   = { path = "../spg-audit" }

[lints]
workspace = true
