/target
**/*.rs.bk
.DS_Store
*.swp
*.swo

# Local dev sandboxes / scratch
/scratch
/.claude/handoff.md
.claude/
/dist/
/references/

# PG / 上游 source 树参考(每个开发者本地 clone,不 commit)
references/

# Internal working material — investigations, designs, correspondence,
# per-release acceptance reports, development process. The public repo
# carries only what a user of SPG needs; everything else lives here and
# is not tracked.
/tmp/
